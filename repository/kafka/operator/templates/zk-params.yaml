NODE_COUNT: "{{ .Params.ZK_NODE_COUNT }}"
MEMORY: "{{ .Params.ZK_MEMORY }}"
MEM_LIMIT: "{{ .Params.ZK_MEM_LIMIT }}"
CPUS: "{{ .Params.ZK_CPUS }}"
CPUS_LIMIT: "{{ .Params.ZK_CPUS_LIMIT }}"
STORAGE_CLASS: "{{ .Params.ZK_STORAGE_CLASS }}"
DISK_SIZE: "{{ .Params.ZK_DISK_SIZE }}"
CLIENT_PORT: "{{ .Params.ZK_CLIENT_PORT }}"
SERVER_PORT: "{{ .Params.ZK_SERVER_PORT }}"
ELECTION_PORT: "{{ .Params.ZK_ELECTION_PORT }}"
